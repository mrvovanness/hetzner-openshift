git clone https://github.com/openshift/openshift-ansible && \
  cd openshift-ansible && \
  git checkout release-3.9 && \
  git pull && \
  cd -
cp .env.example .env
sudo dnf install -y  ansible pyOpenSSL python-cryptography python-lxml ruby
bundle install
